/*const { sendVerificationEmail } = require('./services/mailService');

const testEmail = async () => {
  try {
    const email = 'gamefounder@ukr.net'; // замените на свою тестовую почту
    const verificationCode = Math.floor(100000 + Math.random() * 900000); // случайный код

    await sendVerificationEmail(email, verificationCode);
    console.log(`Email с кодом ${verificationCode} был успешно отправлен на ${email}.`);
  } catch (error) {
    console.error('Ошибка при отправке email:', error);
  }
};

testEmail();*/
